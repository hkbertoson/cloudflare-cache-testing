<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro Cloudflare Auth Testing</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style is:global>
	/* General Reset */
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	/* Body Styling */
	body {
		font-family: 'Arial', sans-serif;
		background-color: #f4f4f9;
		color: #333;
		display: flex;
		justify-content: center;
		align-items: center;
		height: 100vh;
		margin: 0;
	}

	/* Layout Styling */
	.layout {
		text-align: center;
		background: #fff;
		padding: 2rem;
		border-radius: 8px;
		box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
		max-width: 400px;
		width: 90%;
	}

	/* Greeting Text */
	.layout p {
		font-size: 1.2rem;
		margin-bottom: 1.5rem;
		color: #555;
	}

	/* Buttons Styling */
	button {
		display: inline-block;
		padding: 0.75rem 1.5rem;
		font-size: 1rem;
		font-weight: bold;
		color: #fff;
		background-color: #0070f3;
		border: none;
		border-radius: 4px;
		cursor: pointer;
		text-decoration: none;
		transition: background-color 0.3s ease;
	}

	button:hover {
		background-color: #005bb5;
	}

	button.sign-out {
		background-color: #e63946;
	}

	button.sign-out:hover {
		background-color: #c31c2c;
	}

	/* Provider Sign-In Button */
	.auth-astro-components SignIn button {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: 0.5rem;
	}

	/* Responsive Design */
	@media (max-width: 768px) {
		.layout {
			padding: 1.5rem;
		}

		.layout p {
			font-size: 1rem;
		}

		button {
			width: 100%;
		}
	}
</style>
